import * as React from "react";
import "./button.scss";

function Button() {
  return (
    <div className="button" style={{ color: "red" }}>
      <span>button</span>
    </div>
  );
}

export default Button;
